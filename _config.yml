# Site settings
title: Charles.Leung
author: Charles.Leung
email: Charles.xy.liang@gmail.com
description: > # this means to ignore newlines until "baseurl:"
  やり始めば，やり切りなきゃ

domain: "https://charles-liang.github.io" # the domain URL for your site

baseurl: ""  # the relative path that different with domain so you can test locally
base: ""
w3c_url: "/w3c/"  # the base url of css js and images
images_url: "/w3c/images/" # the base url of post images
postfile: "/db/Postfile" # the Database
sitefile: "/db/Sitefile" # the Database
# pagination
page_size: 3


# 3rd URL
jquery_url: "https://cdn.bootcss.com/jquery/3.1.0/jquery.min.js" # use cdn
baiduTemplate_url: "/w3c/js/baiduTemplate.js" #use local
google_analytics_id: ""
baidu_tongji_id: ""

# Build Settings
excerpt_separator: "\n\n" # you can specify your own separator here, default is "\n\n" String
permalink: /:year/:month/:day/:title.html
highlighter: rouge
markdown: kramdown
kramdown:
  input: GFM
  hard_wrap: true # a newline in markdown text would be changed to <br>
gems:
  - jemoji    # enable the github emoji
version: 1.1.0

# Following is got in Github Help With Configuring Jekyll

# Github Page Default Settings which you can override:
# kramdown:
#   input: GFM
#   hard_wrap: false
# gems:
#   - jekyll-coffeescript
#   - jekyll-paginate

# Github Page override the following settings which you cannot change:
# lsi: false
# safe: true
# source: [your repo's top level directory]
# incremental: false
# highlighter: rouge
# gist:
#  noscript: false
# kramdown:
#  math_engine: mathjax
repository: 'charles-liang/charles.liang.github.io'

